# Arquitectura

La aplicación se separa en frontend React, backend FastAPI y base de datos MySQL. La comunicación se realiza mediante API REST JSON protegida con JWT para rutas autenticadas.

Las decisiones técnicas completas se documentan durante la implementación de los requisitos.

